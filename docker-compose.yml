# Локальный запуск приложения из Docker-образа.
# Хранилище in-memory — БД не используется.
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      OWNER_TOKEN: "dev-token"
